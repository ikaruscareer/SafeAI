jinja2==3.1.6
jsonschema==4.26.0
PyYAML==6.0.3
urllib3==2.7.0
# referencing (jsonschema) needs typing-extensions on Python < 3.13; pin it
# explicitly so the hash-locked lockfile installs cleanly on all CI Pythons.
typing-extensions>=4.4.0
