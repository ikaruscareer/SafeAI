# Safe Examples

Examples that demonstrate **safe** AI agent patterns.

## What these demonstrate:
- Properly configured tools with timeouts and retries
- Human approval gates on sensitive actions
- No hardcoded secrets
- Authentication configured on all endpoints
- Safe prompt construction with proper delimiters
- Least-privilege capability design

Add `.py`, `.json`, or `.yaml` files showing safe agent configurations.
