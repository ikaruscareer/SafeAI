"""Opt-in, local-first usage telemetry for SafeAI.

Telemetry is disabled by default. It is only activated when a user explicitly
enables it via `safeai telemetry on` or the `SAFEAI_TELEMETRY=1` environment
variable. No data is ever transmitted without explicit opt-in.

See `PRIVACY.md` for the full data contract.
"""
